/*
Take as input str, a string. Write a recursive function which returns a 
new string in which all duplicate consecutive characters are separated by 
a ‘ * ’. E.g. for “hello” return “hel*lo”. Print the value returned
*/

#include<iostream>
#include<string>
using namespace std;

// char dupli(char s[], )

int main() {

    char s[100];
    int l = sizeof(s)/sizeof(char);
    cout<<l;

    // cin>>s;
    // cout<<s;

	return 0;
}